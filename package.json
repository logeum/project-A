{
  "name": "project-a",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "dev:worker": "wrangler dev",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=project-a && wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "wrangler": "^3.80.0"
  }
}
